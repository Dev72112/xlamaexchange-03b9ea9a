/**
 * Instant Exchange Feature Components - Barrel Export
 * 
 * Note: Components are re-exported from their original locations
 * for backwards compatibility during migration
 */

// Core instant exchange components  
export { ExchangeForm } from '@/components/exchange/ExchangeForm';
export { CurrencySelector } from '@/components/exchange/CurrencySelector';
export { PriceChart } from '@/components/exchange/PriceChart';
