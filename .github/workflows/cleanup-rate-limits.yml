name: Daily Rate Limits Cleanup

on:
  schedule:
    # Run daily at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Call cleanup-rate-limits edge function
        run: |
          response=$(curl -s -w "\n%{http_code}" -X POST \
            "https://mnziekrimlmvnrcwgmbk.supabase.co/functions/v1/cleanup-rate-limits" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}")
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          echo "Response: $body"
          echo "HTTP Status: $http_code"
          
          if [ "$http_code" -ge 200 ] && [ "$http_code" -lt 300 ]; then
            echo "✅ Rate limits cleanup completed successfully"
          else
            echo "❌ Rate limits cleanup failed with status $http_code"
            exit 1
          fi
